
  # iOS Control Center Prototype

  This is a code bundle for iOS Control Center Prototype. The original project is available at https://www.figma.com/design/fFUDgEER0Lbf950n0uWaQm/iOS-Control-Center-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  